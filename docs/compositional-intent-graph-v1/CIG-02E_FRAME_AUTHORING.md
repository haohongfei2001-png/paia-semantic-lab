# CIG-02E provenance-bound predicate frames: first public source shard

This source-authoring checkpoint adds typed `ACTION`, `OBJECT`, and `OUTCOME` phrases for the eight D02 Family & Relationships Topics. Each entry names its formal Topic ID, public profile shard, exact Chinese and English `semantic_core` basis, and allowed source classes. The role phrases were authored from those public meanings with general semantic reasoning. The CI validator checks exact source linkage, complete D02 coverage, bounded phrase lists, and formal Catalog membership.

The file is explicitly marked `PARTIAL_SOURCE_AUTHORING_NOT_RUNTIME_CANDIDATE`. It covers 8/144 Topics and is not compiled into the router. No DEV or capability TEST was opened or read for this checkpoint. The earlier CIG-02B/C/D unqualified outcomes remain unchanged. This source-only work does not establish routing precision, coverage, macro recall, safety, or footprint.

Continue source authoring for the remaining public profile shards, preserve all 144 Topic eligibility, and only then consider a compact deterministic compiler with global composition rules. A new source-separated public DEV covering all 144 Topics and ambiguous/DEFER controls is still required before CIG-02 can freeze. CIG-03 remains blocked.
