# CIG-02E provenance-bound predicate frames: public source authoring

This source-authoring checkpoint adds typed `ACTION`, `OBJECT`, and `OUTCOME` phrases for 96 Topics across the complete first four public profile shards (D01-D12), now including Travel & Places, Media & Culture, and Communication & Writing. Each entry names its formal Topic ID, public profile shard, exact Chinese and English `semantic_core` basis, and allowed source classes. The role phrases were authored from those public meanings with general semantic reasoning. The CI validator checks exact source linkage, complete D01-D12 coverage, bounded phrase lists, and formal Catalog membership.

Each source file is explicitly marked `PARTIAL_SOURCE_AUTHORING_NOT_RUNTIME_CANDIDATE`. The collection covers 96/144 Topics and is not compiled into the router. Frame authoring and its validator read no DEV rows. Existing Actions regression jobs still score their already registered public DEV; no new DEV or capability TEST was opened, and no scored row informed these frames. The earlier CIG-02B/C/D unqualified outcomes remain unchanged. This source-only work does not establish routing precision, coverage, macro recall, safety, or footprint.

Continue source authoring for the two remaining public profile shards, preserve all 144 Topic eligibility, and only then consider a compact deterministic compiler with global composition rules. A new source-separated public DEV covering all 144 Topics and ambiguous/DEFER controls is still required before CIG-02 can freeze. CIG-03 remains blocked.

D03 frames are routing source data only. They do not make medical claims, diagnose conditions, or provide treatment guidance.

D08 frames are routing source data only. They do not contain financial advice or investment recommendations.
