# REM-01 — Diagnostic Harness and Representation Probes

Build deterministic diagnostics for oracle-candidate attribution, Topic descriptor variants derived from existing catalog fields, allowed-context representation, chunk/multi-vector behavior, fusion-source contribution and resource/truncation behavior.

Use legacy calibration only through family-grouped development folds. Do not use the consumed SEM-07 lockbox to rank configurations.

No new owner labels, formal Topic mutation, training or production integration.

Exit artifacts must identify which representation families are technically viable and reproducible; REM-02 may become READY. STOP.
