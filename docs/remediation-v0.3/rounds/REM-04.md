# REM-04 — Router Remediation and Pre-Lockbox Freeze

Prerequisite: REM-03 candidate promotion PASS.

With the candidate configuration frozen, remediate only non-trained Router scoring, evidence aggregation, calibration use and abstention logic.

Development uses legacy calibration; legacy evaluation is bounded promotion evidence. Consumed SEM-07 lockbox is not a tuning or promotion set.

At close, freeze code, model revisions, representations, candidate configuration, Router policy and thresholds. No new labels. REM-05 may become READY only after the pre-lockbox manifest is immutable. STOP.
