# REM-05 — New Independent Personal Lockbox Construction

Requires fresh explicit read-only authorization for a PAIA snapshot. No historical authorization is inherited.

Before labels, select a fixed 96-case batch: 64 representative + 32 challenge, all family-disjoint from every v0.2 gold family. No label-adaptive top-up.

After batch freeze, perform one model-identity-blind semantic adjudication pass. Clear cases may use ChatGPT only under fresh explicit owner delegation; genuine boundary ambiguities may be surfaced to the owner.

No remediation code/config changes after labeling begins. Private gold stays outside Git. STOP.
