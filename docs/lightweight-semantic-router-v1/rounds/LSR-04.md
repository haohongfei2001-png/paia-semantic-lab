# LSR-04 — Frozen Private Calibration Validation

Status: BLOCKED / PRIVATE / NOT_AUTHORIZED.

Requires explicit owner authorization after LSR-03 PASS and a new pre-evidence
freeze. May read exactly the existing 80 calibration records once.

The 13 legacy evaluation records remain closed. The consumed 35-case SEM-07
lockbox remains forbidden for tuning/promotion.

Calibration failure stops the round; it does not authorize tuning and rerunning
on the same 80 records.
