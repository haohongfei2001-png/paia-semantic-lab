# LSR-05 — Legacy Evaluation

Status: BLOCKED / PRIVATE / NOT_AUTHORIZED.

Requires LSR-04 PASS plus a separate explicit owner authorization.

May read the 13 still-unopened legacy evaluation records exactly once. No
candidate reselection, feature changes or threshold changes are permitted after
the evaluation is opened.
